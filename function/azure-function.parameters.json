{
    "$schema": "http://schema.management.azure.com/schemas/2015-01-01/deploymentParameters.json",
    "contentVersion": "1.0.0.0",
    "parameters": {
        "appServicePlanName": {
          "value": "#{LOCAL.AppServicePlan}"
        },
        "functionAppName": {
          "value": "#{LOCAL.FunctionAppName}"
        },
        "appInsightsName": {
          "value": "#{LOCAL.AppInsights}"
        },
        "storageAccountName": {
          "value": "#{LOCAL.StorageAccountWebJobs}"
        },
        "serviceBusName": {
          "value": "#{LOCAL.ServiceBusName}"
        }
    }
}